# Azure / LLM (placeholders - do not commit secrets)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=

# Figma MCP (resolved via VS Code MCP config; this is for reference only)
FIGMA_MCP_URL=
